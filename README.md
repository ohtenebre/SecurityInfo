# Docker
```bash
# Сборка образа
docker build -t lab .

# Запуск контейнера
docker run -it --rm lab
```

# Venv
```bash
# Создать venv
python3 -m venv .venv

# Активировать venv
source .venv/bin/activate

# Скачать нампай
pip install numpy

# Запвстить питон
python main.py
```